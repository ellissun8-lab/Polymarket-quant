# std0-quant environment variables (no secrets required: all data sources are public)
# Copy this file to `.env` and adjust if needed. Never commit real secrets.

# Trader under study (frozen research subject)
STD0_WALLET=0xdf7930e89a2c47560165331863c31deca0733dcd
STD0_NAME=std0

# Optional overrides for API endpoints (defaults live in config/settings.yaml)
# POLYMARKET_DATA_API=https://data-api.polymarket.com
# POLYMARKET_GAMMA_API=https://gamma-api.polymarket.com
# POLYMARKET_WS_URL=wss://ws-subscriptions-clob.polymarket.com/ws/market
# BTC_WS_URL=wss://stream.binance.com:9443/ws/btcusdt@trade

# Optional HTTP(S) proxy if direct access to the data sources is blocked
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
